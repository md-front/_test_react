import initialState from '../initialParams';
import {

} from '../types/app';

const app = (state = [], action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default app
